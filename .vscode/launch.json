{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Tooling Demo",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/examples/tooling_demo.py",
            "console": "integratedTerminal",
            "justMyCode": true,
            "cwd": "${workspaceFolder}",
            "env": {
                "PYTHONPATH": "${workspaceFolder}:${env:PYTHONPATH}"
            }
        }
    ]
}
